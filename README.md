# AI Agent CLI

A conversational command-line AI agent — like a tiny Cursor or Windsurf — that takes a natural-language instruction in your terminal, reasons through it step by step, and writes real files to disk.

It is **general-purpose**: ask it to clone any public website, scaffold a portfolio, build a small game, or generate a static landing page. The headline demo (matching the assignment) is cloning the **Scaler Academy** website — the agent fetches `scaler.com`, studies the real markup, and emits a working `scaler-clone/index.html` + `style.css` + `script.js` with a header, hero, and footer that visually resemble the real site.

---

## Features

- **Conversational REPL** — chat in the terminal, keep iterating on the same project ("make the hero darker", "add a pricing section").
- **Reasoning loop** — `START → THINK → TOOL → OBSERVE → OUTPUT`. Every step is one JSON object; the runtime executes the tool, feeds the result back, repeats.
- **Real tools the agent can call:**
  - `fetchUrl(url)` — GETs any public URL and returns visible HTML/text (script/style/comments stripped, truncated). The agent uses this to read the actual target site before generating a clone.
  - `createFolder(path)`
  - `writeFile({ path, content })` — primary file-creation tool (lenient JSON parser handles small-model escape quirks)
  - `writeFileBase64({ path, content_base64 })` — escape-free fallback for very long files
  - `readFile(path)`, `listDir(path)`, `executeCommand(cmd)`
  - `getTheWeatherOfCity(city)`, `getGithubDetailsAboutUser(username)` (kept from the assignment baseline)
- **Free LLM backend** — Groq's `llama-3.3-70b-versatile` via the OpenAI-compatible endpoint (free key from [console.groq.com](https://console.groq.com/keys), 30 RPM, no card needed).
- **Robust runtime:**
  - Path sandboxing — every file op is resolved under the project root, so the agent can never write outside it.
  - JSON extraction that strips markdown fences and parses multiple objects from a single reply.
  - Lenient `writeFile` parser that recovers when the model produces slightly malformed JSON for big HTML/CSS payloads.
  - History trimmer keeps the prompt under 60 KB so long conversations don't blow the context window.
  - Rate-limit safe: `MIN_DELAY_MS` throttle and exponential-backoff retry on 429/503.
  - Credit-protection caps: max 30 model calls per turn, abort after 4 consecutive tool failures.

---

## Project structure

```
ai-agent-cli/
├── .env.example          # GROQ_API_KEY template
├── .gitignore            # excludes .env, node_modules, .DS_Store
├── README.md
├── package.json
├── package-lock.json
├── index.js              # the entire agent (~290 lines)
└── scaler-clone/         # generated by the agent (committed as proof of output)
    ├── index.html
    ├── style.css
    └── script.js
```

---

## Setup

```bash
git clone https://github.com/kushaltalati/scaler-clone-agent.git
cd scaler-clone-agent
npm install
cp .env.example .env
# open .env and paste your free Groq key from https://console.groq.com/keys
npm start
```

### Optional `.env` knobs

```
GROQ_API_KEY=...                 # required
MODEL=llama-3.3-70b-versatile    # default
MIN_DELAY_MS=1500                # min ms between API calls
```

---

## Usage

```
$ npm start

AI Agent CLI — chat with the agent. Type 'exit' to quit.
Model: llama-3.3-70b-versatile
Try: "clone https://www.scaler.com" or "build a portfolio site for a designer".

you > clone the scaler academy website
```

Sample run:

```
[START]  Cloning the Scaler Academy site into scaler-clone/.
[THINK]  Plan: 1) fetchUrl scaler.com to read real headings/nav  2) createFolder
         scaler-clone  3) write index.html, style.css, script.js  4) listDir to verify.
[TOOL]   fetchUrl(https://www.scaler.com)
[OBS]    SCALER ACADEMY ... PROGRAMS MASTERCLASS ALUMNI ... Build a Career You Can …
[TOOL]   createFolder(scaler-clone)
[OBS]    Folder ready: scaler-clone
[TOOL]   writeFile({"path":"scaler-clone/index.html","content":"<!doctype html>…"})
[OBS]    Wrote 4770 bytes → scaler-clone/index.html
[TOOL]   writeFile({"path":"scaler-clone/style.css","content":":root{…"})
[OBS]    Wrote 6807 bytes → scaler-clone/style.css
[TOOL]   writeFile({"path":"scaler-clone/script.js","content":"…"})
[OBS]    Wrote 941 bytes → scaler-clone/script.js
[TOOL]   listDir(scaler-clone)
[OBS]    index.html
         script.js
         style.css

[OUTPUT] Done. Open scaler-clone/index.html in your browser.
```

You can keep chatting on the same session:

```
you > add a testimonials section under the hero
you > build a separate folder called todo-app with a working to-do list
you > exit
```

Then open `scaler-clone/index.html` (or any folder it produced) in your browser.

---

## How the loop works

Each turn, the model returns one JSON object like:

```json
{ "step": "TOOL", "tool_name": "writeFile", "tool_args": "{\"path\":\"…\",\"content\":\"…\"}" }
```

The runtime in [`index.js`](./index.js):

1. Sends the conversation to Groq's llama-3.3-70b with `response_format: { type: "json_object" }` and a small throttle to stay under the 30 RPM free-tier cap.
2. Extracts JSON objects from the reply (strips ` ```json ` fences if the model wraps them).
3. Pushes the parsed step into history as the assistant's message.
4. If `step === "TOOL"`, looks up the tool, executes it, captures stdout/return value, and pushes a synthetic `OBSERVE` user message back to the model.
5. Loops until `step === "OUTPUT"` or a safety cap fires (max 30 calls, 4 consecutive tool errors).

Path safety: every `writeFile` / `readFile` / `createFolder` resolves under `process.cwd()` and refuses anything that would escape the project root.

---

## Submission checklist (Scaler Academy assignment)

- [x] Conversational CLI agent (terminal-native, accepts free-form instructions)
- [x] Reasoning loop with multiple steps and tool calls
- [x] Generates real `index.html`, `style.css`, `script.js`
- [x] Output resembles scaler.com (header + hero + footer, dark navy/blue palette, responsive)
- [x] Public GitHub repo: https://github.com/kushaltalati/scaler-clone-agent
- [ ] YouTube demo video (2–3 min): _add link here_

---

## License

MIT.
